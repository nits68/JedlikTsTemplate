{
  "signal": "SIGHUP",
  "env": {
    "NODE_ENV": "development"
  },
  "watch": ["input.json", "favicon.ico", "forras.txt", "src/"],
  "ext": "ts, js, txt, json, ico",
  "delay": 1000,
  "exec": "tsc && node --inspect ./dist/app.js || exit 1"
}