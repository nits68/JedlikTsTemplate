{
    "signal": "SIGHUP",
    "env": {
        "NODE_ENV": "development"
    },
    "watch": ["favicon.ico", "src/"],
    "ext": "ts, js, txt, json, ico",
    "delay": 0,
    "exec": "./node_modules/.bin/tsc && node --inspect ./dist/App.js  || exit 1"
}
